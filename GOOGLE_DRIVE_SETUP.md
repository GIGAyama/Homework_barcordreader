# Googleドライブ同期のセットアップ

「職員室のPCで名簿を編集し、教室のiPadでそのまま使いたい」を叶えるための設定です。
**アプリを配る人が一度だけ行います。使う先生は、あとから何も設定しません。**

設定が済むと、先生用メニュー →「設定」タブに「Googleでログインして接続」のボタンが出て、
押すだけで同期がはじまります。

未設定でもアプリは動きます。同期の欄が「この配信では同期が使えません」になるだけで、
児童の受付も、ファイルへのバックアップ・復元も、これまでどおり使えます。

⚠️ **ここで扱う「クライアントID」は秘密の鍵ではありません。**
Web 向けの OAuth クライアントIDは、どのみち配信物とブラウザの通信に出てくる公開の
識別子で、知られてもデータは読めません。なりすましを防いでいるのは Google Cloud 側の
「承認済みの JavaScript 生成元」の設定です。だから配信物に埋めてかまいません。
一方「クライアントシークレット」は本物の鍵です。このアプリでは使いませんし、
リポジトリにも置かないでください。

---

## 1. Google Cloud プロジェクトを作る

[https://console.cloud.google.com/](https://console.cloud.google.com/) を開き、
プロジェクトを新しく作ります（例: `shukudai-post`）。無料です。

## 2. Google Drive API を有効化する

「APIとサービス」→「ライブラリ」で **Google Drive API** を探し、「有効にする」を押します。

## 3. OAuth 同意画面を設定する

「APIとサービス」→「OAuth 同意画面」で User Type を **外部** にして作成します。
アプリ名（例: 宿題ポスト）とサポートメールを入れます。公開ステータスは
**「テスト」のままで構いません**（審査は要りません）。

「**テストユーザー**」に、使う先生方の Google アカウントを追加します（最大100名）。
ここに入っていないアカウントではログインできません。

## 4. OAuth クライアントID（ウェブアプリ）を作る

「APIとサービス」→「認証情報」→「認証情報を作成」→「**OAuth クライアント ID**」。

- アプリケーションの種類: **ウェブ アプリケーション**
- **承認済みの JavaScript 生成元** に、配信するURLのオリジンを追加します
  - 独自ドメイン: `https://homework-barcordreader.giga-school.com`
  - GitHub Pages: `https://<ユーザー名>.github.io`
  - 手元で試すとき: `http://localhost:5173`
- 「承認済みのリダイレクトURI」は要りません

作成後に出る `〜〜〜.apps.googleusercontent.com` が **クライアントID** です。

⚠️ ここで登録し忘れたオリジンからは、ログインの時点で弾かれます。
独自ドメインと GitHub Pages の両方で配るなら、両方を登録してください。

## 5. リポジトリに登録する

GitHub の
**Settings → Secrets and variables → Actions → Variables → New repository variable**
で登録します。

- 名前: `VITE_GOOGLE_CLIENT_ID`
- 値: 手順4のクライアントID

⚠️ **Secrets（秘密）ではなく Variables（変数）に置いてください。**
上に書いたとおり公開の識別子なので隠す必要がなく、Secrets に入れると
ビルドのログで伏字になって、埋めこめたかを目で確かめられなくなります。

登録したら、`main` へ何か push するか、Actions から `Deploy to GitHub Pages` を
手で走らせてください。次の配信から有効になります。

## 6. 手元で試すとき

```bash
cp .env.example .env
# .env の VITE_GOOGLE_CLIENT_ID= に値を書く
npm run dev
```

`.env` は `.gitignore` に入れてあるので GitHub には上がりません。

Service Worker は本番ビルドでだけ登録されます。配信物に近い形で確かめるなら
`npm run build` → `npm run preview` を使ってください。

## 7. 使えているかを確かめる

1. 配信されたアプリを開き、先生用メニュー →「設定」タブを開きます。
2. 「複数端末でのデータ同期（Googleドライブ）」に「**Googleでログインして接続**」の
   ボタンが出ていれば、埋めこめています。
3. 「この配信では同期が使えません」と出ているなら、まだ埋まっていません。
   手順5の登録と、そのあとに配信し直したかを確かめてください。

配信物の中を直接見るなら、ビルドした JavaScript にクライアントIDの文字列が
入っているかを探しても分かります。

```bash
grep -o "[0-9a-z-]*\.apps\.googleusercontent\.com" dist/assets/*.js | head -1
```

---

## よくあること

**「このアプリはGoogleで確認されていません」と出る**
公開ステータスが「テスト」のときに出ます。ご自身で作ったプロジェクトなら
「詳細」→「（アプリ名）に移動」で続けられます。手順3でテストユーザーに
追加したアカウントかどうかも確かめてください。

**ログインしようとすると弾かれる**
手順4の「承認済みの JavaScript 生成元」に、いま開いているURLのオリジンが
入っていない可能性が高いです。`https://` から先を、ドメインまで一致させてください。

**2台目の端末に、1台目のデータが出てこない**
接続したあと「復元」か「今すぐ同期」を押してください。自動同期をONにしておくと、
次からは起動時に確認します。

**以前、端末ごとにクライアントIDを貼っていた**
その値は端末の中に残っていて、リポジトリに登録するまでの引き継ぎとして使われます。
登録したあとは、リポジトリの値が優先されます。

**同期を使わない場合**
何もしなくて構いません。記録は端末の中に残り、「バックアップを保存」で
ファイルとして持ち出せます。
